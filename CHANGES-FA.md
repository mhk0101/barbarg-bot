# رفع باگ «سرور مشغول» قبل از لود کامل صفحه

مشکل گزارش‌شده: در «حساب‌های باربگ» ← دکمه‌ی دریافت خودکار اطلاعات (آیکن دانلود)،
به‌محض باز شدن مرورگر و قبل از اینکه فرم لاگین کامل لود شود، برنامه اشتباهاً
پیام «سرور مشغول است» می‌داد و مرورگر بسته می‌شد.

## ریشه‌ی مشکل

مسیر دریافت اطلاعات از `src/automation/engine/step1-engine.js` تابع
`importLastBarname` استفاده می‌کند. در حالت سریع (`fast: true` که
API به‌صورت پیش‌فرض فعال می‌کند) تابع `iSettle` فقط منتظر
`domcontentloaded` و حذف لایه‌ی loading می‌ماند و خیلی زود برمی‌گشت.
بلافاصله بعد از آن `isServerBusy(page)` صدا زده می‌شد. در آن لحظه:

- فیلدهای `#NationalCode`/`#user-password` هنوز به DOM اضافه نشده‌اند،
- ممکن است یک متن موقت شامل یکی از کلمات کلیدی مثل «سرور در حال حاضر...»
  روی صفحه باشد.

تابع قبلی `isServerBusy` صرفاً چک می‌کرد که یکی از `BUSY_PATTERNS` در
بدنه‌ی صفحه باشد و فرم لاگین/باربرگ حاضر نباشد — که این دومی در همان
لحظه برقرار بود و در نتیجه false-positive می‌داد.

## تغییرات اعمال‌شده در `src/automation/engine/step1-engine.js`

۱) **تابع `isServerBusy` بازنویسی شد:**
   - به‌جای یک بررسی واحد، ۳ بار با فاصله‌ی ۳ ثانیه دوباره می‌سنجد
     (`confirmMs = 3000`). اگر در هر کدام از بررسی‌ها فرم لاگین ظاهر
     شد یا الگوی «مشغول» از بین رفت، نتیجه `false` است.
   - شروط سخت‌گیرانه‌تر:
     * اگر متن صفحه بیشتر از ۱۵۰۰ کاراکتر باشد، صفحه‌ی خطای سرور نیست
       (صفحات خطای IIS/ASP.NET کوتاه‌اند).
     * اگر هرکدام از نشانه‌های صفحه‌ی واقعی لاگین در HTML دیده شود
       (`NationalCode`, `user-password`, `dntCaptcha`, `کد ملی`,
       `رمز عبور`, `ورود به سامانه`, ...)، مشغول نیست.
     * اگر فرم لاگین یا فرم باربرگ حاضر است، قطعاً مشغول نیست.
   - الگوها با `Server Error in`, `Runtime Error`, `503` و
     «چند دقیقه دیگر مجددا» گسترش یافتند تا خطاهای واقعی بهتر تشخیص
     داده شوند.

۲) **تابع سریع `iSettle` اکنون یک سلکتور انتظار می‌پذیرد:**
   - امضای جدید: `iSettle(label, waitForSelector)`.
   - اگر `waitForSelector` داده شود، حداکثر ۵ ثانیه (۲۵ × ۲۰۰ms)
     منتظر ماندن آن عنصر در DOM می‌ماند.
   - در `importLastBarname` بعد از باز کردن صفحه‌ی ورود،
     `await iSettle('صفحه‌ی ورود', '#NationalCode')` صدا زده
     می‌شود تا مطمئن شویم فرم لاگین واقعاً آماده شده، **قبل از**
     اینکه چک «سرور مشغول» انجام شود.

این تغییرات فقط در مسیر سریع (دکمه‌ی دریافت اطلاعات) اثر می‌گذارند و
مسیر عادی/آرام (`fast: false` یا `runWaybill`) از `settlePage` قبلی
استفاده می‌کند که خودش `networkidle` و مکث انسانی دارد.

با این اصلاح، اگر سایت واقعاً سالم باشد، فرم لاگین فرصت لود شدن پیدا
می‌کند و دیگر false-positive «سرور مشغول» رخ نمی‌دهد. اگر سایت واقعاً
مشغول باشد (صفحه‌ی خطای کوتاه IIS/ASP.NET به‌مدت چند ثانیه پایدار
بماند)، کماکان درست تشخیص داده می‌شود.
