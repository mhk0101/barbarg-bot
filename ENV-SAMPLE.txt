# ---- این فایل را با نام .env در ریشه‌ی پروژه ذخیره کن ----
# (نام فایل باید دقیقاً .env باشد؛ .gitignore جلوی کامیت شدنش را می‌گیرد)

# 1) دیتابیس PostgreSQL  — الزامی
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5432/barbarg?schema=public"

# 2) Redis برای صف BullMQ — الزامی برای ثبت خودکار
#    توجه: پورت پیش‌فرض کد 6380 است نه 6379. اگر ردیس‌ات روی 6379 است همین را بگذار:
REDIS_URL="redis://localhost:6379"

# 3) کلید رمزنگاری پسورد حساب‌های باربرگ — حتماً عوض کن (۳۲ کاراکتر یا بیشتر)
#    اگر این را عوض کنی، پسوردهای قبلاً ذخیره‌شده دیگر باز نمی‌شوند.
BARBARG_PASSWORD_KEY="یک-رشته-تصادفی-طولانی-و-یکتا-اینجا-بگذار-32ch!"

# 4) کلید امضای توکن ورود به پنل — الزامی
JWT_SECRET="یک-رشته-تصادفی-طولانی-دیگر"

# 5) NextAuth
AUTH_SECRET="یک-رشته-تصادفی-سوم"
NEXTAUTH_URL="http://localhost:3000"

# 6) آدرس پایه‌ی API (اختیاری، برای اسکریپت‌ها)
API_URL="http://localhost:3000"
