# راهنمای اجرا و تحویل پروژه‌ی ربات باربرگ

این سند دو بخش دارد: **(الف) چطور پروژه را بالا بیاوری** و **(ب) دقیقاً چه کاری باقی مانده و باید انجام بدهی**.

---

# الف) اجرای پروژه

## پیش‌نیازها

| ابزار | نسخه | چرا لازم است |
|---|---|---|
| Node.js | 20 یا بالاتر | خود پروژه (Next.js 16 / React 19) |
| PostgreSQL | 14 یا بالاتر | تمام داده‌ها (۳۱ جدول) |
| Redis | 6 یا بالاتر | صف کارها (BullMQ) — بدون آن ثبت خودکار کار نمی‌کند |
| Google Chrome | نصب‌شده | ربات با `channel: 'chrome'` باز می‌شود، نه با کرومیوم پلی‌رایت |
| دسترسی به اینترنت ایران | — | `barname.utcms.ir` از خارج ایران باز نمی‌شود (تست شد: timeout) |

نکته‌ی مهم: **این پروژه باید روی یک ماشین با صفحه‌نمایش اجرا شود**، چون مرورگر در حالت `headless: false` باز می‌شود تا لاگین دستی و کپچا را ببینی. روی سرور بدون GUI باید Xvfb راه بیندازی.

## مرحله ۱ — نصب

```bash
git clone https://github.com/mhk0101/barbarg-bot
cd barbarg-bot
npm install
npx playwright install chromium
```

## مرحله ۲ — فایل `.env`

فایل `ENV-SAMPLE.txt` را که کنار همین سند ساختم بردار، محتوایش را در فایلی به نام `.env` در ریشه‌ی پروژه بگذار و مقادیر را پر کن.

متغیرهایی که کد واقعاً می‌خواند: `DATABASE_URL`، `REDIS_URL`، `BARBARG_PASSWORD_KEY`، `JWT_SECRET`، `API_URL`، `NODE_ENV`.

⚠️ **تله‌ی شماره ۱:** در `src/lib/redis.ts` پورت پیش‌فرض ردیس `6380` نوشته شده، نه `6379` استاندارد. اگر `REDIS_URL` را ست نکنی، به پورت اشتباه وصل می‌شود و صف بی‌صدا کار نمی‌کند.

## مرحله ۳ — دیتابیس

```bash
createdb barbarg                 # یا از pgAdmin بساز
npx prisma migrate deploy        # اجرای ۱۴ مایگریشن
npx prisma generate              # ساخت کلاینت
node seed.js                     # ساخت کاربر مدیر
```

کاربر پیش‌فرض پنل: `admin@barbarg.local` / `Admin123456` (اولین ورود مجبور به تغییر رمز می‌شوی).

## مرحله ۴ — اجرا

باید **دو ترمینال جدا** باز باشد:

```bash
# ترمینال ۱ — وب‌اپ
npm run dev            # یا: npm run build && npm start

# ترمینال ۲ — ورکر پردازش صف (بدون این، هیچ باربرگی ثبت نمی‌شود)
npm run worker
```

پنل: <http://localhost:3000>

⚠️ **تله‌ی شماره ۲:** زمان‌بند خودکار اجرا نمی‌شود. مسیر `GET /api/scheduler/tick` وجود دارد ولی هیچ‌کس صدایش نمی‌زند. باید یک cron بگذاری:

```bash
* * * * * curl -s http://localhost:3000/api/scheduler/tick > /dev/null
```

## اسکریپت‌های کمکی

```bash
npm run login              # باز کردن مرورگر برای لاگین دستی و ذخیره‌ی نشست
npm run test:waybill       # تست ثبت یک باربرگ
npm run diagnose:waybill   # ذخیره‌ی HTML/اسکرین‌شات/لیست فیلدهای فرم سایت
npm run manual-run         # اجرای دستی یک جاب
```

## کارهای امنیتی که همین اول باید بکنی

1. `automation-data/sessions/default.json` داخل ریپوی پابلیک کامیت شده و **کوکی نشست زنده‌ی یک حساب واقعی** در آن است. آن حساب را logout/تغییر رمز بده.
2. این‌ها را به `.gitignore` اضافه کن و از تاریخچه پاک کن:
   ```
   automation-data/
   diagnostics/
   *.log
   login-output.txt
   login-error.txt
   build-output.txt
   poc-output.txt
   ```
3. `BARBARG_PASSWORD_KEY` را عوض کن — الان کلید پیش‌فرض داخل `worker/processor.ts` هاردکد شده است.

---

# ب) کاری که باقی مانده

خلاصه‌ی وضعیت: **پوسته کامل است، مغز ناقص.** حدود ۴۰ صفحه‌ی پنل، ۷۰ روت API، ۳۱ جدول دیتابیس و ورکر و صف همه ساخته شده‌اند. چیزی که کار نمی‌کند، دقیقاً همان زنجیره‌ی اصلی است: «یک بار دستی ثبت کن → ربات ۱۰ بار تکرار کند».

## مشکل ۱ — فرم دستی هیچ‌جا ذخیره نمی‌شود ⛔ بحرانی

قلب درخواست تو این است: یک بار دستی ثبت کنی و ربات همان را تکرار کند. **این اصلاً پیاده نشده.**

- `LoginFlow.openManualLogin()` مرورگر را باز می‌کند، منتظر می‌ماند لاگین کنی، و فقط **کوکی‌ها** را در `automation-data/sessions/<accountId>.json` ذخیره می‌کند.
- هیچ کدی وجود ندارد که فیلدهای فرمی که دستی پر می‌کنی را بخواند و نگه دارد.
- ابزار نیمه‌کاره‌اش هست (`src/automation/waybill/captureFormState.ts` که HTML و لیست فیلدها را می‌گیرد) ولی به هیچ دکمه یا API وصل نیست — یک ابزار دیباگ است.

**باید بسازی:** یک حالت «ضبط» — مرورگر باز شود، بعد از اینکه فرم را دستی پر و ثبت کردی، مقدار و سلکتور همه‌ی فیلدها خوانده و به‌عنوان قالبِ همان اکانت/پلاک ذخیره شود. مزیت بزرگش این است که مشکل ۳ را هم خودبه‌خود حل می‌کند.

## مشکل ۲ — دو مسیر داده‌ی موازی و ناهماهنگ ⛔ بحرانی

پروژه دو سیستم قالب دارد که به هم وصل نیستند:

| مسیر | جدول‌ها | وضعیت |
|---|---|---|
| A | `BarbargTemplate` + `QuickRegistrationJob` | صفحه دارد، API دارد، ولی **ورکر هیچ‌وقت نگاهش نمی‌کند** |
| B | `RegistrationProfile` + `Job.profileId` | همان چیزی که `worker/processor.ts` واقعاً می‌خواند |

و بدتر: در `src/app/api/automation/trigger/route.ts` وقتی `action: "trigger"` می‌زنی (همان «۱۰ تا ثبت کن»)، جاب‌ها **بدون `profileId`** ساخته می‌شوند:

```ts
data: Array.from({ length: count }, () => ({
  type: 'REGISTER_WAYBILL', status: 'pending', priority: 0, attempts: 0, maxRetries: 3
}))   // ← هیچ profileId ای ست نمی‌شود
```

ورکر جاب را برمی‌دارد، دنبال `job.profile` می‌گردد، چیزی پیدا نمی‌کند و ثبت شکست می‌خورد. **این تک‌خطی‌ترین دلیلی است که ثبت خودکار کار نمی‌کند.** باید یکی از دو مسیر را انتخاب و دیگری را حذف کنی، و `profileId` را به جاب‌ها پاس بدهی.

## مشکل ۳ — سلکتورهای فرم حدسی هستند 🟠 مهم

در `WaybillFlow.ts` سلکتورها آرایه‌ای از حدس‌ها هستند:

```ts
selectors: ['#SenderNationalId', '#senderNationalId', '[name="SenderNationalId"]']
```

هیچ نشانه‌ای نیست که این‌ها با HTML واقعی سایت تطبیق داده شده باشند. ضمناً کامنت خود کد می‌گوید سایت عوض شده و در مرحله‌ی سوم، خودرو و راننده باید از **لیست ثبت‌شده (dropdown)** انتخاب شوند نه تایپ دستی. اگر سلکتورها غلط باشند، فرم پر نمی‌شود.

**باید:** `npm run diagnose:waybill` را روی سایت واقعی بزنی، فایل‌های `diagnostics/*.json` را نگاه کنی و سلکتورها را با واقعیت تطبیق بدهی. (حالت ضبط در مشکل ۱ این را خودکار می‌کند.)

## مشکل ۴ — استعلام سوخت هیچ اسکرپی ندارد 🟠

`/api/fuel` فقط از جدول `FuelCard` خودمان می‌خواند. **هیچ کدی نیست که به سایت وصل شود و سهمیه را بخواند.** گفتی «از خود سایت است، سینکش کنی اوکیه» — یعنی این ماژول کلاً باید نوشته شود: یک flow پلی‌رایت که با همان نشست، صفحه‌ی سوخت را باز کند، مقادیر را دربیاورد و در `FuelCard`/`FuelLog` بریزد.

## مشکل ۵ — SMS فوروارد تست نشده 🟡

طبق اعتراف خود `README-SMS-FORWARD.md`، توسعه‌دهنده نمی‌دانسته سایت چه نوع لینکی می‌فرستد. `SmsLinkFlow` فقط لینک را باز می‌کند و اسکرین‌شات می‌گیرد — اگر لینک نیاز به کلیک تأیید داشته باشد، کار نمی‌کند. باید با یک پیامک واقعی تست و تکمیل شود.

## مشکل ۶ — گزارش هر پلاک ناقص 🟡

`/api/plate-analytics` هست ولی گزارش تجمیعی «هر پلاک: چند ثبت موفق، چند ناموفق، چه خطاهایی، چقدر سوخت» که خواستی، به شکل یکپارچه وجود ندارد. نسبتاً ساده است و بعد از حل ۱ و ۲ باید ساخته شود.

## مشکل ۷ — کنترل تعداد در سطح پلاک اعمال نمی‌شود 🟡

`LicensePlate` فیلدهای `dailyTarget` و `dailyCount` دارد، ولی ورکر هنگام ثبت این‌ها را نه چک می‌کند نه به‌روز می‌کند. سقف روزانه فقط در سطح `RegistrationProfile.registrationsPerDay` و آن هم فقط در scheduler اعمال می‌شود (که خودش اجرا نمی‌شود — تله‌ی ۲).

---

# ترتیب پیشنهادی کار

1. **محیط را بالا بیاور** (بخش الف) و مطمئن شو پنل باز می‌شود و ورکر بالا می‌آید.
2. **یک حساب باربرگ ثبت کن** و با `npm run login` نشست بساز — تا اینجا باید بدون کدنویسی کار کند.
3. **`npm run diagnose:waybill`** بزن و ساختار واقعی فرم را دربیاور. این ورودیِ همه‌ی کارهای بعدی است.
4. **مشکل ۲ را حل کن** (وصل کردن `profileId` به جاب‌ها) — کم‌ترین کد، بیش‌ترین اثر. بعد از این باید بتوانی اولین ثبت خودکار موفق را ببینی.
5. **مشکل ۱ را بساز** (حالت ضبط فرم دستی) — این همان «اصل کار» است.
6. **مشکل ۳** را با خروجی مرحله ۳ تطبیق بده.
7. بعد: گزارش پلاک (۶)، سقف روزانه (۷)، سوخت (۴)، SMS (۵).

---

# جمع‌بندی صادقانه

وقتی گفتند «ربات آماده است، فقط فرم‌ها را سینک کن»، دقیق نیست. سینک فرم‌ها یکی از کارهاست، ولی زنجیره‌ی «ضبط دستی → تکرار خودکار» که اصل محصول است، اصلاً نوشته نشده و مسیر داده‌ی جاب‌ها هم شکسته است. تخمین واقع‌بینانه برای رساندن به یک ثبت خودکار پایدار: **مشکل ۲ چند ساعت، مشکل ۱ و ۳ چند روز کار** (وابسته به اینکه سایت چقدر با dropdownهای سفارشی سخت باشد). ماژول سوخت و SMS جدا و بعد از آن.

نکته‌ی آخر که به‌عنوان مجری پروژه باید بدانی: ثبت انبوه خودکار در سامانه‌ی دولتی همراه با دور زدن کپچا و OTP، به‌احتمال زیاد خلاف شرایط استفاده‌ی سامانه است و اگر بارنامه‌ها صوری باشند می‌تواند تبعات قانونی داشته باشد. تعیین تکلیف این موضوع با کارفرما قبل از شروع، به نفع خودت است.
