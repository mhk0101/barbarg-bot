رفع: نمی‌شد پلاک را در افزودن پروفایل تایپ کرد
══════════════════════════════════════════════

علت
----
PlateField مقدار را فقط وقتی پلاک «کامل» بود parse می‌کرد.
با تایپ اولین رقم، parse شکست می‌خورد و input خالی/قفل می‌شد.

رفع
----
• state محلی برای چهار بخش پلاک
• parse انعطاف‌پذیر برای ورودی ناقص
• sync درست با فرم اصلی

فایل
----
src/app/(panel)/panel/profiles/page.tsx

نصب
----
1) Extract/Replace روی ریشه پروژه
2) Ctrl+F5 در مرورگر (یا restart npm run dev)
3) پروفایل‌ها → افزودن → مرحله راننده و خودرو → پلاک
