تغییرات نهایی v3 (طبق درخواست دقیق شما)
════════════════════════════════════════════
۱) در fillLocationStep(): متغیر province از provinceFromPlate() در اولویت قرار گرفت.
۲) تابع select2Direct() صبرهای طولانی‌تری دارد:
   - ۱.۲ ثانیه برای باز شدن dropdown
   - ۵۰۰ میلی‌ثانیه برای تمرکز input
   - ۲.۵ ثانیه برای آمدن لیست AJAX (بعد از تایپ)
۳) تایپ مستقیماً در input.select2-search__field انجام می‌شود.
۴) لیست <ul id="select2-MapCity-results"> / <ul id="select2-MapCity2-results"> خوانده شده و انتخاب می‌شود.
۵) اگر لیست خالی باشد (موردی یافت نشد)، علت در reason برمی‌گردد.

نحوه کار دستی = نحوه کار ربات:
- کلیک روی .select2-selection → dropdown باز می‌شود
- تایپ در input → لیست AJAX می‌آید
- صبر کوتاه → کلیک روی <li>
